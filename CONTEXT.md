# 术语表 / Glossary

这份文件是 DuiBi 的**共享词汇**。代码、注释、提交信息、以及和我（或任何接手的人）
的对话都用这里的词，不要换成同义词——名字不一致是最便宜也最常见的沟通损耗来源。

Every term below is the word the code actually uses. Prefer it over synonyms.

---

## 对比模型 / The comparison

### Row（行）
两栏共同渲染的一个**对齐单位**，不是文件里的一行。`DiffRow` 记录这一行在左侧、
右侧分别对应哪一行——任一侧都可以为空。

这是整个 UI 的地基：两栏共用同一份 `Vec<DiffRow>`，所以第 `i` 行在左右两栏
处于同一个 `y`。**不要**说 "line" 来指 row。

### Line（行）
文档里真实的一行文本，有行号。一个 row 最多对应每侧各一个 line。

### Side（侧）
`Left` 或 `Right`。永远不要用 "pane index"、"column number"、"0/1" 来代替。

### Filler（占位行）
某一侧没有 line 的 row。用斜纹绘制，**不是空行**——它是为了对齐而存在的空白。
这个区分很重要：用户曾经因为占位行和空行长得像，以为「删除空行」没生效。

### Hunk（差异块）
连续的一段发生变化的 row。合并箭头以 hunk 为单位工作。空行造成的 `Ignored`
row 不会打断一个 hunk。

### RowKind（行的性质）
`Equal` / `Replace`（两侧都有但不同）/ `Insert`（只有右侧）/ `Delete`（只有左侧）/
`Ignored`（被「忽略空行」跳过的空行）。

配色约定：删除=红、新增=绿、**修改=琥珀色**。修改单独用一种颜色，是为了让
「这行被改过」和「这行被删了」一眼可分。

### Alignment key（对齐键）
`DiffOptions` 中真正影响**行如何配对**的那部分。`granularity` 不在其中——它只
决定改动怎么涂色。改 granularity 必须只重绘、不重新对比，否则视图会跳。

---

## 编辑器 / The editor

### Pane（栏）
一侧的编辑器控件。两个 pane 共享一份 `RowLayout` 和一份 `Viewport`。

### RowLayout（行几何）
每个 row 的高度与纵向位置。关闭自动换行时是纯算术；开启时用 Fenwick 树支撑
变高行。两条不变式：

- 一个 row 的高度 = **两侧折行后的较高者**（各侧测量分开存，后画的不能覆盖先画的）
- 绘制期间的测量**下一帧才生效**（否则先画的栏和后画的栏依据不同几何，行号会错开）
- 两栏折行宽度相差不足一个字符时，**都按较窄者折行**（`aligned_wrap_width`）。
  看不出差别的宽度差也足以改变长行的折点，行高取 max 后较宽一栏底部就会空出一段

### Viewport（视口）
两栏共同看向哪里。拥有共享滚动偏移、当前由哪一栏主导、以及待执行的跳转。
不依赖 egui，可以当普通逻辑测试。

### Anchor（锚点）
重新对比**之前**记下视口顶部是哪一行，之后把它放回原位。因为对比可能在视口
上方增删占位行，而偏移是按像素算的。

### Reveal（显示方式）
`Centre`（居中，用于跳到没看过的地方）或 `Minimal`（最小移动，用于让光标保持可见）。

### Seam（缝）
可以替换实现而不改动调用处的位置。本项目最重要的一条缝是 `core/` 与 `ui/`
之间：`core/` 完全不知道 egui 的存在。

---

## 文本 / The text

### TextBuffer（缓冲区）
一个 pane 的文档。内部是 rope（真相来源）+ 就地打补丁的行向量（供对比与渲染
直接借用）。所以在第 9 万行编辑和在第 3 行一样快。

### Selection（选区）
`anchor`（起点，拖动时不动）+ `head`（光标现在的位置），单位是**字符**索引，
不是字节。

### Vertical motion（上下移动方式）
`KeepColumn`（保持列，默认，大多数编辑器的行为）或 `LineEnd`（落到目标行行尾）。
**与差异高亮粒度无关**——粒度只决定改动怎么涂色。两者曾经被误认为是一回事，
所以现在一个在「视图」菜单、一个在「对比」菜单，且各自的说明都点明了这一点。

### Goal column（目标列）
上下移动时光标想保持的列。必须跨帧保存——否则穿过一个短行就丢了。

### Composition / pre-edit（输入法组合串 / 预编辑）
用输入法打字时，「nihao」这段还没定下来的拼音就是 pre-edit（预编辑串）。
它**不进文档**，存在 `ui::ime::Composition` 里，由编辑器画在光标处。
只有 commit（上屏）才写进缓冲区。理由见下面第 7 条决定。

### Display column vs. line column（显示列 / 行内列）
制表符展开成空格后位置会变。`tabs::to_display` / `from_display` 负责换算。
命中测试和光标定位都走**真实排版出来的 galley**，不要用「字符宽度 × 列数」估算。

---

## 用词禁忌 / Words to avoid

| 不要用 | 用 |
|---|---|
| line（指对齐单位时） | **row** |
| pane index / column 0 / 1 | **side** |
| blank line（指对齐空白时） | **filler** |
| boundary / API | **seam** / **interface** |
| block / chunk / region | **hunk** |
| granularity（指光标时） | 它只指**高亮粒度**，光标用 **vertical motion** |

---

## 已经定下来的设计决定

以下决定有明确理由，重新提议前请先读理由：

1. **egui 而非 Dioxus** —— 需要自绘的虚拟滚动编辑器；webview 里要和
   `contenteditable` 争夺光标、选区和输入法。`core/` 不依赖 GUI，真要换框架
   可以整体搬过去。

2. **rope + 行向量双份存储** —— 文本在内存里存两遍。换来的是编辑代价与光标
   深度无关，且对比引擎可以直接借用 `&[String]`。

3. **语法高亮用检查点 + 窗口缓存 + 时间切片** —— syntect 是顺序解析器，本项目
   实测约 125µs/行（`regex-fancy` 纯 Rust 后端的代价）。三条机制缺一不可：
   - 每 256 行存一份解析器快照，绘制任意窗口只需从最近检查点往前
   - 已解析的窗口（含前后各 256 行余量）缓存起来，窗口内滚动零开销
   - **所有解析都按 8ms 时间片分帧进行，且半途状态会保存**。限额必须按*时间*
     算而不是按行数——行的解析成本差一个数量级，按行数限额曾造成单帧 661ms 卡死

4. **对比有时间预算** —— 单次对比 250ms 超时后降级成较粗的结果，不阻塞界面。

5. **占位行画斜纹** —— 仅靠底色深浅区分不了占位行和真空行。

6. **中日韩字体从系统加载** —— egui 自带字体没有 CJK 字形。附加为 fallback，
   Latin 仍用自带的等宽字体。

7. **输入法预编辑串不进文档** —— egui 自带的 `TextEdit` 是把预编辑串直接插进
   缓冲区再整段替换。这里不这么做，原因有三：每个拼音字母都会变成一条撤销记录；
   每个字母都会让文档版本号 +1，从而把整份 14000 行的对比重跑一遍；光是「开始
   打字又反悔」就会把文档标记成已修改。
   预编辑串因此存在 `ui::ime::Composition`（不依赖 GUI 框架，可单测）。
   另外：**编辑器每帧必须上报 IME 区域**（`PlatformOutput::ime`）。集成层用
   `let allow_ime = ime.is_some();` 决定是否 `set_ime_allowed`——不上报就等于
   把窗口的输入法关掉，中文一个字都打不进来。`tests/ime_input.rs` 守着这一点。

8. **两栏共用一个行号栏宽度** —— 取两份文档中较长的那份来定宽（`gutter_lines`
   由 app 层算一次，传给两个 pane），不再各栏按自己的行数算。
   原因：文字宽度是 `pane_w - gutter_w - 16`，行号栏宽一变文字宽度就变，相同的
   长行就会在两栏折成不同段数；而 row 高度取两侧较高者，折得少的那侧底部就多出
   一条空白——看着像凭空多了一行，实际两侧内容完全一致。
   `aligned_wrap_width` 的容差只有一个字符宽，**只能盖住差 1 位数字的情况**
   （999 vs 1000，而且是踩在浮点比较的刀尖上侥幸盖住的）；差 2 位就必然漏
   （999 vs 10000，实测 200 个窗口宽度里 68 个分叉）。所以从源头统一,不靠容差。
   附带好处：两侧行号栏等宽，文字起始位置对齐。`tests/gutter_alignment.rs`
   用**扫描一段宽度区间**的方式守着——单一固定宽度的测试会假通过，因为折点只在
   两个折行宽度正好跨过边界时才移动。
